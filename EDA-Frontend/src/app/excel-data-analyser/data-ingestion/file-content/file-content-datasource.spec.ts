import { FileContentDatasource } from './file-content-datasource';

describe('FileContentDatasource', () => {
  it('should create an instance', () => {
    expect(new FileContentDatasource()).toBeTruthy();
  });
});
